server:
  port: ${PORT:8083}

spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://db.xnmwuyrmhwydunpnxzom.supabase.co:5432/postgres?sslmode=require&connectTimeout=10}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Swaraj@0405}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      connection-timeout: 60000
      leak-detection-threshold: 60000
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:paramjitbaral44@gmail.com}
    password: ${MAIL_PASSWORD:bsxo htpy odbz zuhu}
    properties.mail.smtp.auth: true
    properties.mail.smtp.starttls.enable: true

jwt:
  secret: ${JWT_SECRET:dev-secret-kl-smartq}
  expiration: ${JWT_EXPIRATION:3600}

cors:
  allowed-origins: ${CORS_ORIGINS:http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:5173,http://localhost:8082}
